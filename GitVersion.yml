mode: ContinuousDeployment
tag-prefix: '[vV]'
continuous-delivery-fallback-tag: 'pre'
branches:
  # v1_develop:
  #   mode: ContinuousDeployment
  #   tag: pre
  #   regex: ^v1_develop?[/-]
  #   is-release-branch: false
  #   source-branches:
  #   - v1
  # v1:
  #   tag: rc
  #   increment: Patch
  #   regex: ^v2?[/-]
  #   is-release-branch: false
  #   source-branches: []
  #   is-mainline: true

  v2_develop:
    mode: ContinuousDeployment
    tag: pre
    regex: ^v2_develop?[/-]
    is-release-branch: false
    tracks-release-branches: true
    source-branches:
    - v2_develop
  v2:
    mode: ContinuousDeployment
    is-release-branch: true
    tag: alpha
    increment: Patch
    regex: ^v2?[/-]
    is-source-branch-for: ['v2_develop']
    source-branches: []

  # feature:
  #   tag: useBranchName
  #   regex: ^features?[/-]
  #   source-branches:
  #   - v1
  #   - v1_develop
  #   - v2
  #   - v2_develop
 
  pull-request:
    tag: PullRequest.{BranchName}
    increment: Inherit
ignore:
  sha: []
merge-message-formats: {}
